.DS_Store
# Per-user plugin settings (contains the password hash/verifier) — never commit.
data.json
# Compiled Touch ID helpers (users build locally with native/build.sh).
native/Obsidian
native/touchid-auth
node_modules/
